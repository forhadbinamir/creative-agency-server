import React from 'react';
import inb from '../../images/brandIcons/airbnb.png'
const Contact = () => {
    return (
        <div>
            <div><img src={inb} alt="" /></div>





            {/* <div onClick={() => setOpen(!open)} className='w-10 h-10 md:hidden pr-3 text-white '>
                {open ? <XIcon></XIcon> : <MenuIcon></MenuIcon>}
            </div>
            <ul className={`flex flex-col md:flex-row justify-end w-full absolute md:sticky duration-300 ease-in ${open ? 'top-10' : 'top-[-220px]'}`}></ul> */}
        </div>
    );
};

export default Contact;